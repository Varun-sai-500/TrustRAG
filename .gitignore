# ─── Secrets & environment ────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.cert
*.crt
secrets/

# ─── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
!apps/web/src/lib/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
*.whl
MANIFEST
*.manifest
*.spec
pip-log.txt
pip-delete-this-directory.txt
.tox/
.nox/
.hypothesis/
.pytest_cache/
instance/
.webassets-cache
.scrapy
docs/_build/
.pybuilder/
target/
profile_default/
ipython_config.py
.pdm.toml
.pdm-python
.pdm-build/
__pypackages__/
celerybeat-schedule
celerybeat.pid
*.sage.py
/app
.venv
.venv*
apps/api/.venv/
env/
venv/
ENV/
env.bak/
venv.bak/
.spyderproject.db
.spyproject
.ropeproject
/site
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
cython_debug/

# ─── Coverage / test output ──────────────────────────────────────────────────
.coverage
.coverage.*
coverage.xml
coverage/
htmlcov/
apps/web/coverage/
apps/api/coverage/
*.cover
*.py,cover
nosetests.xml
test-results/
junit.xml
playwright-report/
blob-report/
playwright/.cache/
test-results/.last-run.json

# ─── Ruff cache ───────────────────────────────────────────────────────────────
.ruff_cache/

# ─── Node / Frontend ──────────────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
apps/web/dist/
apps/web/dist-ssr/
apps/web/*.local
.vite/
.vitest/
.npm
.eslintcache
.stylelintcache
*.tsbuildinfo
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# ─── Root package lock (accidental installs) ──────────────────────────────────
# Root-level package.json/lock are ignored — deps live in apps/web and apps/api
/package.json
/package-lock.json

# ─── IDE ──────────────────────────────────────────────────────────────────────
.idea/
.vscode/
.history/
*.code-workspace
*.swp
*.swo
*~
.project
.classpath
.settings/
*.sublime-project
*.sublime-workspace
.cursor/
.windsurf/
.claude/

# ─── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini
.AppleDouble
.LSOverride

# ─── Logs ─────────────────────────────────────────────────────────────────────
*.log
logs/
*.out
*.err

# ─── Docker ───────────────────────────────────────────────────────────────────
.docker/
# Local compose overrides (personal ports/volumes) must never be committed.
docker-compose.override.yml
docker-compose.override.yaml

# ─── Runtime data (Qdrant collections, caches, model snapshots) ────────────────
data/
apps/api/data/

# ─── ML / Model cache (sentence-transformers, HuggingFace Hub, GGUF) ──────────
.cache/
.cache/huggingface/
.model_cache/
apps/api/.model_cache/
models/
*.bin
*.pt
*.ckpt
*.safetensors
*.gguf
*.onnx
*.onnx.data
*.llamafile
llama-server.log
.huggingface/

# ─── Security scan output ─────────────────────────────────────────────────────
bandit-report.json
safety-report.json
trivy-report.json
trivy-results.sarif
semgrep-report.json
pip-audit-report.json

# ─── Package manager lockfiles ────────────────────────────────────────────────
# apps/web uses npm (package-lock.json is committed)
# apps/api uses uv — uv.lock IS committed (the API Dockerfile does `uv sync --locked`)
bun.lockb
uv.lock
!apps/api/uv.lock

# ─── AI / Agentic tool directories ────────────────────────────────────────────
.agents/
.aider*
.continue/
*.aider.chat.history.md

# ─── Jupyter / notebooks (if ever used for evaluation) ────────────────────────
.ipynb_checkpoints/
*.ipynb

# ─── Runtime / process files ──────────────────────────────────────────────────
*.pid
*.seed
*.pid.lock
.python-version
.tool-versions

# ─── Temporary / Scratch files ────────────────────────────────────────────────
*.tmp
*.bak
*.orig
test.txt
test_*.txt
.superpowers/

# ─── Agent memory & machine-local dirs ────────────────────────────────────────
.remember/

# ─── Env-tool locals ─────────────────────────────────────────────────────────
.envrc

# ─── SQLite runtime caches (embedding disk cache, etc.) ───────────────────────
*.sqlite
*.sqlite3
*.db
*.db-wal
*.db-shm

# ─── Evaluation run artifacts (runner JSON output; methodology stays tracked) ──
docs/evaluation/results/

# ─── k6 / load-test raw output ────────────────────────────────────────────────
load-test/*.har
load-test/results/

